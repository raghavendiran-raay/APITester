name: Validating API
on:
  push:
    branches: ["Python", "Node"]
  pull_request:
    branches: ["Python", "Node"]

jobs:
  run-tests:
    uses: ./.github/workflows/github_actions.yml@main
    secrets: inherit
